#!/bin/bash
set -e
cd "$(dirname "$0")/.."

# backend
docker compose run --rm -e DATABASE_URL=postgresql://cc:cc@db:5432/cc app \
  python -m pytest app/tests/ -v "$@"

# frontend
cd frontend && npm run build
