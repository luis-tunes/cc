import { ComingSoon } from "@/components/shared/ComingSoon";
import { Tags } from "lucide-react";
export default function Classifications() {
  return <ComingSoon title="Classificações" subtitle="Centro de classificação SNC, mapeamento de contas e categorização assistida por IA" icon={Tags} />;
}
